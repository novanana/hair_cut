# 헤어 도해도 노트 — PRD (Product Requirements Document)

## 0. 한 줄 요약
미용학원 수업 중 **스마트폰으로 즉시 두상 도해도를 그리고, 메모를 남기고, 자동 저장**할 수 있는 모바일 전용 웹앱(PWA).

---

## 1. 배경 및 문제 정의

미용학원 수업(커트/펌/컬러/업스타일)은 진행 속도가 빠르고, 강사가 시범을 보이는 짧은 시간 안에 두상 도해도(섹션 분할, 시술 각도, 층 구조 등)와 부가 설명을 동시에 기록해야 한다.

기존 대안의 문제:
- **종이 도해도북**: 들고 다니기 불편, 분실 위험, 검색/정리 불가
- **일반 필기 앱(노트/굿노트 등)**: 두상 템플릿이 없어 매번 처음부터 그려야 함 → 느림
- **사진 촬영만**: 그 위에 각도/섹션 표시나 메모를 덧붙이기 어려움

→ **두상 템플릿 위에 빠르게 그리고 메모하는 전용 도구**가 필요.

## 2. 목표 (Goals)

1. 수업 중 **10초 이내**에 새 도해도를 열고 첫 선을 그을 수 있을 것 (진입 속도)
2. 인터넷 연결 없이도 그리기/저장이 100% 동작할 것 (학원 와이파이 불안정 대응)
3. 시술 유형별(커트/펌/컬러/업스타일)로 기록을 빠르게 다시 찾을 수 있을 것
4. 별도 앱 설치 없이 **모바일 브라우저에서 바로 사용**, 원할 경우 홈 화면에 추가(PWA)

### Non-goals (MVP 범위 제외)
- 다중 사용자 협업(실시간 공동 편집)
- 강사-학생 간 콘텐츠 배포/LMS 기능
- 데스크톱 최적화 UI (모바일 전용으로 명시됨)

## 3. 타겟 사용자 & 핵심 시나리오

**타겟**: 미용학원 재학생 (스마트폰 사용, 필기 속도가 중요, 그림 실력과 무관하게 빠른 기록이 목적)

**핵심 시나리오**
1. 강사가 커트 시범 중 두상 뒷면 섹션을 나눔 → 학생이 "뒷면 템플릿"을 선택 → 손가락/스타일러스로 섹션 라인과 각도를 표시 → 옆에 "45도 시술, 크리에이티브 포인트 확인" 메모 → 자동 저장
2. 복습 시 "커트" 태그로 필터링 → 날짜순으로 그날 그린 도해도 목록 확인 → 확대해서 재확인
3. 강사 시범 장면을 사진으로 찍고, 그 사진 위에 선을 겹쳐 그려 설명 추가

## 4. 핵심 기능 (Feature List)

### MVP (1차 출시 필수)
| # | 기능 | 설명 |
|---|------|------|
| F1 | 두상 템플릿 선택 | 앞/옆/뒤/탑 뷰 기본 두상 라인 템플릿 제공 (커트용 기본 4종) |
| F2 | 드로잉 캔버스 | 펜/지우개, 색상 3~4종, 굵기 조절, 실행취소·재실행 |
| F3 | 텍스트 메모 | 캔버스 내 텍스트 박스 삽입(드래그 이동 가능), 캔버스 하단 자유 메모란 |
| F4 | 자동 저장 | 입력 즉시 로컬(IndexedDB)에 자동 저장, 앱 종료/새로고침 후에도 유지 |
| F5 | 목록/정리 | 저장된 도해도를 썸네일 목록으로, 날짜순 정렬, 시술 유형 태그(커트/펌/컬러/업스타일/기타)로 필터 |
| F6 | 오프라인 동작 | 네트워크 없이 전체 기능 동작 (PWA + Service Worker) |
| F7 | 삭제/이름 변경 | 개별 도해도 삭제, 제목 수정 |

### V1.1 (빠른 후속 추가)
| # | 기능 | 설명 |
|---|------|------|
| F8 | 사진 배경 삽입 | 카메라 촬영/갤러리 사진을 배경 레이어로 깔고 그 위에 드로잉 |
| F9 | 도형/화살표 도구 | 직선, 화살표, 각도 표시선 (시술 각도 설명용) |
| F10 | 확대/축소, 손바닥 이동 | 핀치 줌, 팬(pan) — 세밀한 구역 확대해서 그리기 |
| F11 | 즐겨찾기 | 자주 참고하는 도해도 상단 고정 |
| F12 | 이미지/PDF 내보내기·공유 | 카톡/에어드롭 등으로 친구와 공유, 인쇄 대비 PDF |

### V2 (확장, 필요 시)
| # | 기능 | 설명 |
|---|------|------|
| F13 | 클라우드 백업/동기화 | 로그인 시 기기 변경/분실 대비 백업 (Firebase/Supabase) |
| F14 | 사용자 템플릿 등록 | 자주 쓰는 커스텀 템플릿을 직접 추가/저장 |
| F15 | 음성 메모 | 손이 바쁠 때 음성으로 짧은 메모 녹음 → 텍스트 병기 |

> **권장**: F1~F7만으로 실사용 테스트를 먼저 해보고, 실제 수업에서 아쉬운 부분(F8 사진 배경이 유력)을 우선순위로 잡는 것을 추천합니다.

## 5. 기술 스택 제안

모바일 웹, 오프라인 우선, 빠른 드로잉 반응성이 핵심 제약이므로 아래 스택을 권장합니다.

| 영역 | 선택 | 이유 |
|------|------|------|
| 프레임워크 | **React + TypeScript + Vite** | 빠른 개발, PWA 플러그인 생태계(`vite-plugin-pwa`) 성숙 |
| 드로잉 엔진 | **HTML5 Canvas 직접 구현** (또는 `perfect-freehand`로 필압/자연스러운 선 보정) | Fabric.js 등 범용 라이브러리는 무거움. 손그림 앱은 canvas 저수준 제어가 반응속도에 유리 |
| 레이어 구조 | 템플릿(SVG, 고정) + 드로잉(Canvas, 벡터 스트로크 저장) + 텍스트(DOM/HTML 오버레이) | 템플릿은 벡터라 확대해도 깨지지 않음, 스트로크를 좌표 배열로 저장하면 후에 편집/색변경 용이 |
| 로컬 저장소 | **IndexedDB (Dexie.js 래퍼)** | localStorage보다 대용량(이미지 포함) 저장에 적합, 비동기 API |
| PWA | Service Worker + Web App Manifest | "홈 화면에 추가"로 앱처럼 실행, 오프라인 캐싱 |
| 스타일링 | Tailwind CSS | 모바일 반응형 빠르게 구성 |
| (선택) 백엔드 | Supabase (Auth + Postgres + Storage) | V2 클라우드 동기화 시점에 도입, MVP에는 불필요 |
| 배포 | Vercel / Netlify (정적 호스팅 + HTTPS 자동) | PWA는 HTTPS 필수 |

### 왜 네이티브 앱이 아니라 웹(PWA)인가
- 학원 수업 중 "앱스토어 설치" 장벽 없이 URL 하나로 바로 사용 가능 → 요청하신 "모바일 전용 웹페이지"와 부합
- 이후 필요 시 Capacitor로 감싸 네이티브 앱 전환도 가능한 구조로 설계

## 6. 데이터 모델 (초안)

```
Diagram {
  id: string (uuid)
  title: string                // "3/15 커트수업 - 그라데이션"
  category: 'cut' | 'perm' | 'color' | 'updo' | 'etc'
  templateId: string           // 사용한 두상 템플릿
  backgroundImage?: blob        // V1.1: 촬영 사진 배경
  strokes: Stroke[]             // 벡터 드로잉 데이터
  textNotes: TextNote[]
  freeMemo: string              // 하단 자유 메모
  isFavorite: boolean
  createdAt: datetime
  updatedAt: datetime
}

Stroke {
  points: [{x, y, pressure}]
  color: string
  width: number
}

TextNote {
  x, y: number
  content: string
}

Template {
  id: string
  name: string                  // "뒷면 두상", "옆면 두상"
  svgPath: string
  category: string
}
```

## 7. 화면 구성 (모바일 화면 흐름)

```
[홈/목록 화면]
 ┌─────────────────────┐
 │ 헤어 도해도 노트       │  ← 상단: 검색 + 태그 필터(전체/커트/펌/컬러/업스타일)
 │ ┌────┐ ┌────┐ ┌────┐│
 │ │썸네│ │썸네│ │썸네││  ← 그리드/리스트, 최근순
 │ │일  │ │일  │ │일  ││
 │ └────┘ └────┘ └────┘│
 │              [+ 새로만들기] ← FAB 버튼, 우하단 고정
 └─────────────────────┘
        │ 탭
        ▼
[템플릿 선택 (신규 시)]
 ┌─────────────────────┐
 │ 앞  옆  뒤  탑  (사진)│  ← 가로 스크롤 썸네일
 └─────────────────────┘
        │
        ▼
[에디터 화면] ← 핵심 화면, 대부분의 시간을 여기서 보냄
 ┌─────────────────────┐
 │ ← 제목       ⋮ 메뉴  │  ← 상단바(최소화, 자동저장 표시 아이콘)
 │                      │
 │     [두상 템플릿]     │  ← 캔버스 영역 (전체화면 최대화)
 │      + 드로잉         │
 │                      │
 ├─────────────────────┤
 │ 펜 굵기 색상 지우개 ↶↷│  ← 하단 툴바 (엄지 도달 범위 고려)
 ├─────────────────────┤
 │ [자유 메모 입력란]     │  ← 탭하면 확장되는 텍스트 영역
 └─────────────────────┘
```

**UX 원칙**
- 하단 툴바는 **엄지 조작 영역**에 배치 (한 손 사용 전제)
- 저장 버튼 없음 → **입력 즉시 자동 저장** + 우상단에 작은 "저장됨" 인디케이터만 표시
- 새 도해도 생성까지 탭 2번 이내 (FAB → 템플릿 선택)

## 8. 비기능 요구사항

| 항목 | 요구사항 |
|------|----------|
| 성능 | 드로잉 시 입력 지연(latency) 50ms 이하, 60fps 유지 |
| 오프라인 | 네트워크 없이 전체 기능 100% 동작 |
| 저장 안정성 | 브라우저 강제 종료/새로고침에도 데이터 유실 없어야 함 (디바운스 자동저장, 500ms) |
| 반응형 | 세로모드 기준 최적화 (가로모드는 2순위) |
| 브라우저 지원 | iOS Safari, Android Chrome 최신 2개 버전 |
| 데이터 용량 | 로컬 저장 용량 한계 안내 (사진 배경 다수 저장 시 IndexedDB 용량 모니터링 필요) |
| 접근성 | 터치 타겟 최소 44x44px (손가락 조작 기준) |

## 9. 개발 로드맵 (제안)

| 단계 | 기간(추정) | 범위 |
|------|-----------|------|
| Phase 0 | 2~3일 | 프로젝트 세팅 (Vite+React+PWA), 배포 파이프라인 |
| Phase 1 | 1주 | 캔버스 드로잉 엔진 (펜/지우개/undo) + 두상 템플릿 4종 |
| Phase 2 | 3~4일 | 로컬 저장(IndexedDB) + 자동저장 + 목록 화면 |
| Phase 3 | 3~4일 | 텍스트 메모, 태그/카테고리, 검색·필터 |
| Phase 4 | 2~3일 | PWA 마감(오프라인 캐싱, 홈 화면 추가), 실기기 테스트 |
| → MVP 출시 | | 실사용 테스트 시작 |
| Phase 5 (V1.1) | 1주 | 사진 배경 삽입, 도형/화살표 도구, 핀치줌 |
| Phase 6 (V2) | 필요 시 | 클라우드 동기화, 커스텀 템플릿 |

## 10. 리스크 및 고려사항

- **필압/손그림 정확도**: 손가락 터치는 스타일러스보다 부정확 → 펜 굵기 프리셋을 크게(3단계 이상) 제공해 보완
- **로컬 저장 한계**: iOS Safari는 저장 용량 정책이 엄격(무활동 시 데이터 삭제 가능) → 장기적으로 V2 클라우드 백업이 사실상 필수
- **템플릿 다양성 부족 시 이탈**: 초기 두상 템플릿 종류가 실제 수업 커리큘럼과 안 맞으면 무용 → 실제 학원 교재의 도해도 종류를 먼저 조사해 템플릿 셋 확정 필요
- **입력 지연**: React state 기반 리렌더링으로 캔버스를 그리면 끊김 발생 가능 → 드로잉 중에는 canvas 네이티브 API로 직접 그리고, state는 스트로크 완료 시점에만 반영하는 구조 권장

---

### 다음 단계 제안
1. 실제 사용할 두상 템플릿 종류(커트 기준 몇 가지 뷰가 필요한지) 확정
2. Phase 0~1 범위로 바로 프로토타입 구현 착수 가능 (원하시면 지금 코드베이스 세팅부터 시작하겠습니다)
